const browserDevConfig = require('./src/browser.dev.config');
const browserProdConfig = require('./src/browser.prod.config');
const nodeConfig = require('./src/node.config');

module.exports = {
  browserDevConfig,
  browserProdConfig,
  nodeConfig,
};
